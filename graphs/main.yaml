inputs: []
outputs:
  report: save_files.report
nodes:
  config:
    inputs: {}
    outputs: [src_folder, dst_folder]
    provider: hexmod-backup.config
  auth:
    provider: hexmod-backup.auth:run
    inputs:
      redirect_uri: https://tryhexbox.com/callback
    outputs:
      - access_token
      - refresh_token
      - expires_in
      - token_type
  list_files:
    provider: hexmod-backup.list_files:run
    inputs:
      token: auth.access_token
      path: BackMeUp
    outputs:
      - files
  read_files:
    provider: hexmod-backup.read_files:run
    inputs:
      token: auth.access_token
      files: list_files.files
    outputs: [files_content]
  save_files:
    provider: hexmod-backup.save_files:run
    inputs:
      files_content: read_files.files_content
      dst_folder: config.dst_folder
    outputs: [report]
