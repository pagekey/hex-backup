inputs: []
outputs:
  report: schedule.report
nodes:
  auth:
    inputs:
      redirect_uri: https://tryhexbox.com/callback
    outputs:
      - access_token
      - refresh_token
      - expires_in
      - token_type
    provider: hexmod-backup.auth
  schedule_config:
    inputs: {}
    outputs: [src_folder, dst_folder, daily_time]
    provider: hexmod-backup.schedule_config
  schedule:
    inputs:
      src_folder: schedule_config.src_folder
      dst_folder: schedule_config.dst_folder
      daily_time: schedule_config.daily_time
      token: auth.access_token
    outputs: [report]
    provider: hexmod-backup.schedule
